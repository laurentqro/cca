<section class="hero is-dark is-small">
  <div class="hero-body">
    <div class="navbar-end">
      <% if current_user.present? %>
        <p class="navbar-item">Bonjour <%= current_user.first_name %> (<%= t("activerecord.attributes.user.group.#{current_user.group}") %>)</p>
        <% if current_user != true_user %>
          <div class="navbar-item">
            <div class="field is-grouped is-grouped-multiline">
              <p class="control">
                <%= link_to "Redevenir #{true_user.first_name}", stop_impersonating_users_path, method: :post, class: "button is-primary" %>
              </p>
            </div>
          </div>
        <% end %>

        <%= link_to "Editer mon profil", edit_user_registration_url, class: "navbar-item" %>
        <%= link_to 'Déconnexion', destroy_user_session_url, method: :delete, class: "navbar-item" %>
      <% end %>
    </div>

    <div class="container">
      <h1 class="title">
        <%= link_to 'CCA', root_path %>
      </h1>
      <h2 class="subtitle">
        Partage de fichiers
      </h2>
    </div>
  </div>

  <div class="hero-foot">
    <div class="container">
      <%= render 'navbar' %>
    </div>
  </div>
</section>
